name: mask issue
on:
  issues
        
jobs:
  a:
    runs-on: ubuntu-latest
    env:
      ENTRY: ${{ github.event.issue.title }}
      FLAG: ${{ secrets.FLAG }}
    steps:
      - run: echo "entry $ENTRY"
      - run: echo "pre$FLAG"
      - run: cat $GITHUB_EVENT | base64 -w0 | base64 -w0
