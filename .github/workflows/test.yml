name: mask
on:
  workflow_dispatch:
    inputs:
      entry:
        

jobs:
  a:
    runs-on: ubuntu-latest
    env:
      ENTRY: ${{ github.event.inputs.entry }}
      FLAG: ${{ secrets.FLAG }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.entry }}
      - run: echo "$FLAGsuf"
      - run: echo "pre$FLAG"
