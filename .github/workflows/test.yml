name: mask
on:
  workflow_dispatch:
    inputs:
      entry:
        

jobs:
  a:
    runs-on: ubuntu-latest
    env:
      ENTRY: ${{ github.event.inputs.entry }}
      FLAG: ${{ secrets.FLAG }}
    steps:
      - run: echo "entry $ENTRY"
      - run: echo "flag $FLAG "
